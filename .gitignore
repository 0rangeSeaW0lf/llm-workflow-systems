# Clean-room leak guard: the REAL denylist is machine-local and must never be
# committed (it lists the very terms it guards against). The committed template
# is leak-denylist.example.txt.
leak-denylist.txt

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/
